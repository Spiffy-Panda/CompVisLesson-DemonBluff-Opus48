# utils — durable tooling catalog

Promoted, human-named scripts that something *depends on* (they build artifacts, regenerate tracked content, or run often enough to deserve a stable name). The opposite of `scrap_scripts/` (throwaway, gitignored).

**Promotion rule (Rule 1):** a scrap script graduates here the moment anything other than a human-at-the-CLI or an LLM agent depends on it. On promotion: drop the `NN_` prefix, give it a descriptive name and a header comment, anchor it to the repo root, and **add a row to the table below**.

Split by language: `utils/python/`, `utils/powershell/`, etc.

## Catalog

| Script | Language | Purpose | Depended on by |
|--------|----------|---------|----------------|
| [`python/serve_site.py`](python/serve_site.py) | Python (stdlib) | Local static web server for `site/` on `0.0.0.0` (no-cache, prints LAN URL). For previewing the site locally; GitHub Pages serves the same `site/` via Actions. | Local preview workflow; `.github/workflows/deploy-pages.yml` mirrors what it serves |
| [`python/run_pipeline.py`](python/run_pipeline.py) | Python (cv2, numpy, stdlib) | Offline CLI runner for the full CV pipeline (gate → localize → identify) on sampled PNGs in `dataset/frames/`. Builds the card-art gallery once, runs `run_pipeline` on each frame, prints per-frame summary (frame_state, card identities + confidences), writes snapshot JSON, and optionally saves annotated overlay PNGs to `dataset/pipeline-out/`. No HTTP server required. | Lesson plan hands-on demos; `tests/test_cli.py` tests its pure helpers |
| [`python/mine_card_crops.py`](python/mine_card_crops.py) | Python (cv2, numpy, onnxruntime, stdlib) | **DEV/BATCH ONLY (never on the REST path).** Round-2 dataset miner: streams both raw sample videos through the Stage 0 selector (`dbcv.frame_select.iter_selected_frames`, board frames only), localizes cards with `classical_localize`, saves per-slot crop PNGs to `dataset/crops/<Sample>/`, and writes `dataset/crops/manifest.jsonl` — one line per crop with BOTH identifier proposals (classical confidence + embedding margin/abstention), an agreement flag, and a `label: null` field for the wave-2 labeling pass. | Round-2 labeling / margin-calibration / face-down-inventory passes consume `dataset/crops/` + the manifest (see `plans/PLAN-round2-dataset.md`) |
| [`python/finetune_embedding.py`](python/finetune_embedding.py) | Python (torch, torchvision, onnxruntime, numpy) | **DEV-ONLY (torch + GPU).** Generates the **SERVED** embedding model `models/mobilenetv3_small_embed.onnx` (+ `.pt`) by domain-fine-tuning MobileNetV3-Small on the 43 card characters: **Proxy-Anchor** loss (implemented inline), **LP-FT** (proxy warm-up frozen → top-4 blocks unfrozen), synthetic augmentation from the reference art. Reports inter-prototype cosine before/after (0.85→0.41) and torch↔ONNX parity (~5.5e-6). Re-run after any model change or on an art swap. | `src/dbcv/embed.py` loads the ONNX output at runtime; `tests/test_embed.py` skips if file absent |
| [`python/export_backbone.py`](python/export_backbone.py) | Python (torch, torchvision, onnxruntime, numpy) | **DEV-ONLY.** Exports the **FROZEN-ImageNet BASELINE** MobileNetV3-Small (classifier head stripped) to ONNX at `models/mobilenetv3_small_embed_frozen.onnx`, kept only for the fine-tune head-to-head. Validates torch↔onnxruntime parity (< 1e-4 max abs diff). Requires torch + torchvision (not in the runtime requirements). | The frozen-vs-fine-tuned eval (`utils/python/embed_eval.py`); not loaded by the runtime |
| [`python/wiki_harvest.py`](python/wiki_harvest.py) | Python (stdlib only — urllib) | **OUT OF DATE (2026-07-29)** — current wiki layout sweeps ~45 images per villager page (the all-roles icon strip lands next to the real art; see `_drop-off/villager_new_harvest/`); needs an `*-Icon.png` filter before regenerating `card-art/`. Fetch-once harvester for the Demon Bluff wiki (MediaWiki API, not scraping; polite UA + delays). Regenerates the gitignored `knowledge-base/wiki/_raw_cache/` and `knowledge-base/card-art/<class>/` plus the **tracked** `harvest-manifest.json`. **Required on any fresh clone** — the card-art gallery the pipeline/tests build from is gitignored. Promoted 2026-07-29 (was `scrap_scripts/python/01_wiki_harvest.py`). | Gallery build (`src/dbcv/gallery.py` reads `card-art/`), tests, fine-tune training data; fresh-machine bootstrap (`README.md`) |
| [`python/embed_eval.py`](python/embed_eval.py) | Python (cv2, numpy, onnxruntime) | Head-to-head eval of classical vs embedding identifiers on real board frames from `dataset/frames/` — the "honest eval" the fine-tune workflow ends with. Promoted 2026-07-29 (was `scrap_scripts/python/09_embed_eval.py`). | `finetune_embedding.py` directs its user here for the real-frame check; round-2 fine-tune eval (`plans/PLAN-round2-dataset.md`) |
| [`python/grab_frame.py`](python/grab_frame.py) | Python (cv2, pywin32, windows-capture) | **Capture-box only.** Grab exactly ONE frame of the live Demon Bluff window via Windows Graphics Capture and save it as a PNG (hardened window finding: title match verified against the owning `demon bluff.exe` process; per-monitor DPI awareness; WxH read from the frame, never hard-coded). Promoted 2026-07-29 (was `scrap_scripts/python/04_grab_frame.py`). | Live collection / play-agent workflows (per-action state grabs); `record_session.py` shares its window-finding code |
| [`python/record_session.py`](python/record_session.py) | Python (cv2, numpy, pywin32, windows-capture) | **Capture-box only.** Continuous WGC session recorder: 30–60 min live gameplay to **constant-frame-rate** mp4 (CFR resampled on a writer thread — capture callback never touches disk; `avc1`→`mp4v` codec fallback; segment-per-resize) plus `frames.jsonl`/`session.json` sidecars carrying per-frame timestamps and the wall-clock↔frame-index alignment epoch. `--game-version` required (footage-version-drift rule). Recorded video feeds Stage 0 (`src/dbcv/frame_select.py`) exactly like the sample videos. **Never open the recorded mp4s into context.** | Live-session dataset collection (`dataset/live_*/`); Stage 0 frame selection consumes its mp4 output; future action-log alignment (weak temporal labels) |
